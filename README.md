# StudentGo - AI Assistant Integration

## 🚀 Быстрый старт

### 1️⃣ Установка зависимостей
В папке `PracticeWork` откройте терминал и выполните:
```bash
npm install
```

### 2️⃣ Запуск сервера
```bash
npm start
```

Вы должны увидеть:
```
🚀 Server running on http://localhost:3000
📡 API ready at http://localhost:3000/api/chat
🏥 Health check at http://localhost:3000/api/health
```

### 3️⃣ Откройте приложение
Откройте `index.html` в браузере (например, через Live Server или просто двойной клик)

### 4️⃣ Используйте чат
- Нажмите кнопку 💬 в правом нижнем углу
- Напишите сообщение и нажмите Enter или кнопку отправки
- ИИ асистент ответит на ваш вопрос

---

## 🔧 Что было установлено

### Сервер (Node.js)
- `server.js` - Express сервер с API для чата
- `package.json` - зависимости проекта
- `.env` - конфигурация с GROQ_API_KEY (защищен в .gitignore)

### Фронтенд
- Добавлен чат UI в `index.html`
- JavaScript функции для отправки сообщений
- Красивый CSS с поддержкой темного режима

---

## ⚠️ Важно

- **Сервер должен запущен** на порту 3000 для работы чата
- **API токен защищен** - хранится только на сервере, не видим в браузере
- **Разговоры сохраняются** в памяти сервера (очищаются при перезагрузке)

---

## 🐛 Если что-то не работает

1. **Откройте консоль браузера** (F12 > Console)
2. **Проверьте сервер**:
   ```bash
   curl http://localhost:3000/api/health
   ```
   Должна вернуться: `{"status":"ok","message":"Server is running","groqKeyPresent":true}`

3. **Посмотрите логи в терминале сервера** - там будут ошибки

4. **Убедитесь** что сервер запущен перед открытием браузера

---

## 📝 Структура проекта
```
PracticeWork/
├── index.html          # Главное приложение с чатом
├── server.js           # Node.js Express сервер
├── package.json        # Зависимости
├── .env                # Конфигурация (GROQ API KEY)
├── .gitignore          # Защита .env от git
└── node_modules/       # Установленные зависимости
```

---

## 🎯 Модели Groq которые доступны

- `mixtral-8x7b-32768` (по умолчанию) - быстрая и мощная
- `llama2-70b-4096` - большая модель
- `gemma-7b-it` - легкая модель

Можно изменить в `server.js` строке с `model:` 

---

**Готово к использованию! 🎉**
